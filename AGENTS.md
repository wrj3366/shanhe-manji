# 山河慢记 · 项目约定

这是长期维护的私人旅行路书与日志工程。开始规划、修改或复盘前先读：
1. `data/preferences.json`：带来源与状态的偏好；
2. 对应 `trips/<id>/trip.json`、`plan.md`、`journal.json`、`review.md`；
3. 涉及伊犁经验时，读 `notes/travel-field-notes.md` 原始记录。

## 规划与记忆
- 用户当前要求优先于旧偏好。`confirmed` 是已表达偏好，`context` 只适用特定行程，`inferred` 只能作为可修正的建议，`wishlist` 不代表去过。
- 不把每天300—500公里当配额。分别写纯驾驶、休息、游玩及假期拥堵；连住、飞行加自驾可用来节省精力。
- 喜欢历史古迹、地方美食、沿途自然风景；“轻松”不能变成没有游玩内容。
- 规划、真实体验与旅后推荐必须分开。日期过去不能自动证明行程已完成；未出行的路书不自动生成日记。
- 人数、预算、车型、具体地址、航班和是否实际到访均不能猜。当前两人一车预算只是某次方案假设。
- 新偏好只有在用户明确表达或可靠历史反馈支持时写入，并记录来源和状态；不要把一次天气、酒店或景点取舍升级为永久规则。
- 既有参考图片不是用户实拍；媒体需要标记来源、类型和使用权限。不要自动把外链图片归为个人照片。

## 每次行程如何存放
- `trips/<YYYY-MM-slug>/trip.json`：日期、状态、标签、路线概览与页面链接。
- `plan.md`：约束、计划、关键取舍、待核实信息。
- `journal.json`：用户实际反馈、照片说明与记录；每条保留来源。
- `review.md`：计划与实际的差异、值得重游的地方、下次改法。
- 新页面放该行程的 `page/`，不要再新增根目录独立路书。历史页面路径保留，避免旧链接失效。
- `assets/library-data.js` 是生成文件，改完源JSON后运行 `python3 scripts/travel.py build`。
- 新建行程使用 `python3 scripts/travel.py new <id> --title '<标题>'`；新增真实反馈可用 `note` 子命令，详见 README。

## 保存与校验
- 首页为旅行档案入口，不以浏览器 localStorage 作为唯一记忆。长期数据保存在仓库文本文件，可检索、可版本管理。
- 本地网页以展示为主，新增资料通过对话或CLI写入仓库；不要设计“保存成功”却没有写回文件的表单。
- 修改后运行 `python3 scripts/travel.py check` 与 `build`，必要时跑 `python3 -m unittest discover -s tests`。
- 保留无关用户改动。按本次授权提交相关文件；没有明确要求时，不推送、发布或把家庭信息公开。
- 公开发布时只选择拟分享的路书页面，另行核对偏好、家庭区域、日志和实拍是否适合分享。`notes/`、`trips/`、`data/`不是默认公开资料。
- 本项目使用 GitHub Pages 发布网页，仓库为 `wrj3366/shanhe-manji`；推送 `main` 后由 `.github/workflows/pages.yml` 构建 `dist/` 并部署。用户没有指定更换平台时沿用 GitHub Pages。
